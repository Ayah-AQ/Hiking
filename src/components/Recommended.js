// import hikingTrips from "../hikingTrips";

// const Recommended = (props) => {
//   const y = hikingTrips.filter(
//     (hikingTrip) => hikingTrip.difficulty === props.x
//   );
//   return console.log(<h1>{props.x.name}</h1>);
// };

// export default Recommended;
